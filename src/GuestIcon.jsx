// When the user drags a GuestIcon component onto a Table, the table's 
// status will update (seated) and the GuestIcon will be removed from the 
// Table component. You might use a library like react-dnd for drag-and-drop functionality.

import React from 'react'

export default function GuestIcon() {
  return (
    <div>
        
    </div>
  )
}
