// If a Table is seated, it will change its color to green. 
// If a Table needs bussing, it will turn orange.

import React from 'react'
import GuestIcon from './GuestIcon'

export default function Table() {
  return (
    <div>
        <GuestIcon/>
    </div>
  )
}
